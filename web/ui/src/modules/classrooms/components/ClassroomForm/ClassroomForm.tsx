import React, { FC } from 'react';

type ClassroomFormProps = {
  onSubmit: () => void;
};

const ClassroomForm: FC<ClassroomFormProps> = ({ onSubmit }) => <div>Classroom form</div>;

export default ClassroomForm;
